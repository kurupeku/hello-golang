name: Testing Chapter 05

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
    paths:
      - "subject/chapter05/**.go"

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: './.github/actions/test'
        with:
          target: chapter05
