name: Testing Chapter 02

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
    paths:
      - "subject/chapter02/**.go"

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: './.github/actions/test'
        with:
          target: chapter02
