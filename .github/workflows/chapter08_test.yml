name: Testing Chapter 08

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
    paths:
      - "subject/chapter08/**.go"

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: './.github/actions/test'
        with:
          target: chapter08
