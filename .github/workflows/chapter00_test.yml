name: Testing Chapter 00

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
    paths:
      - "subject/chapter00/**.go"

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: './.github/actions/test'
        with:
          target: chapter00
